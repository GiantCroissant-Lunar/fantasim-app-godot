<Project Sdk="Godot.NET.Sdk/4.7.0">
  <PropertyGroup>
    <TargetFramework>net8.0</TargetFramework>
    <EnableDynamicLoading>true</EnableDynamicLoading>
    <RootNamespace>CodeQuality</RootNamespace>
    <Nullable>enable</Nullable>
    <ManagePackageVersionsCentrally>false</ManagePackageVersionsCentrally>
  </PropertyGroup>
  <ItemGroup>
    <!-- R3 + the vendored R3.Godot addon (addons/R3.Godot) provide GodotFrameProvider.Process,
         ticked each frame by the FrameProviderDispatcher autoload. ManagePackageVersionsCentrally
         is false here, so the version is inline (matches the app CPM pin + the vendored 1.3.1). -->
    <PackageReference Include="R3" Version="1.3.1" />
  </ItemGroup>
  <!-- Step B+ will add app ProjectReferences (App.Resource for ReloadPolicy, contracts/App.Ui,
       PluginArchi.*) + the real reload-collection integration check. -->
</Project>
