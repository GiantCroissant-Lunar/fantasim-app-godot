<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <TargetFramework>net8.0</TargetFramework>
    <RootNamespace>FantaSim.App.World</RootNamespace>
    <AssemblyName>FantaSim.App.World</AssemblyName>
    <ServiceArchiTier>T3</ServiceArchiTier>
    <UnifyStorageBackends>SurrealDb</UnifyStorageBackends>
  </PropertyGroup>
  <ItemGroup>
    <CompilerVisibleProperty Include="ServiceArchiTier" />
    <CompilerVisibleProperty Include="UnifyStorageBackends" />
  </ItemGroup>

  <!-- App.World.Tests constructs WorldHistoryCoordinator with explicit descriptors to prove the startup
       catalog validation contract (duplicate FieldId throws). No other assembly gains access. -->
  <ItemGroup>
    <InternalsVisibleTo Include="App.World.Tests" />
  </ItemGroup>

  <!--
    T3 world orchestrator (zero Godot usings; the Godot presentation work lives in App.World.Presentation.Seam T4).
    App-side T1 contracts are always referenced. The engine refs below switch package<->project by
    UseProjectReferences (see project/Directory.Build.props): when false the published packages are
    referenced; when true the full composition path compiles against fantasim-world `main` projects.
    On the Phase 0 branch the default is true.
  -->
  <ItemGroup>
    <ProjectReference Include="..\App.Common\App.Common.csproj" />
    <ProjectReference Include="..\..\contracts\App.World\App.World.csproj" />
    <ProjectReference Include="..\..\contracts\App.World.Rendering\App.World.Rendering.csproj" />
    <ProjectReference Include="..\..\contracts\App.NodeGraph\App.NodeGraph.csproj" />
    <ProjectReference Include="..\..\contracts\App.Ecs\App.Ecs.csproj" />
    <ProjectReference Include="..\..\contracts\App.Command\App.Command.csproj" />
    <ProjectReference Include="..\App.NodeGraph\App.NodeGraph.csproj" />
    <ProjectReference Include="..\App.World.Composition\App.World.Composition.csproj" />
    <ProjectReference Include="..\App.World.FieldView\App.World.FieldView.csproj" />
    <!--
      App.Ecs PLUGIN (not just contracts): CellElevationModel owns an Arch ECS world built from the
      app's primitive cell components (CellId/PlateId/CrustSample/CellUnit/Elevation) and runs
      CellElevationSystem over it. Those types are always compiled and have no engine dependency.
    -->
    <ProjectReference Include="..\App.Ecs\App.Ecs.csproj" />
  </ItemGroup>

  <!--
    Cross-repo cartography consumption (dev-mode project references, mirroring the fantasim-world
    engine refs above). GlobePlateSurfaces (T3, Godot-free) feeds the tessellation + per-cell heights
    into the cartography GlobeSurfaceBuilder to produce WATERTIGHT per-plate caps. The cartography
    projects carry their own Directory.Build/Packages.props (UnifyMaths.Numerics 1.0.0, same feed line),
    so they compile self-consistently under a project reference — no CPM/TFM conflict (both net8.0).
    NOT packed to the NuGet feed (publishing needs owner approval / out of scope). These refs are
    UNGATED: unlike the engine refs there is no published cartography package to fall back to, and the
    watertight surface is the app's geometry now, not an optional co-dev path.

    NOTE (2026-07-03 rendering-contracts extraction): the rendering types that consumed cartography
    (GlobePlateSurfaces, ProvinceTint, VertexTintJitter, VertexColorEnvelope) moved to the shared
    contracts/App.World.Rendering project, which carries these cartography refs. The App.World PLUGIN
    keeps its cartography refs for now — the plugin still pulls the cartography assemblies into its
    compile scope transitively via App.World.Rendering, and the explicit refs document the dependency
    for the bundle staging audit. They can be removed once the bundle staging is re-verified.
  -->
  <ItemGroup>
    <ProjectReference Include="$(YokanProjectsRoot)\fantasim-cartography\project\contracts\Cartography.Shared\Cartography.Shared.csproj" />
    <ProjectReference Include="$(YokanProjectsRoot)\fantasim-cartography\project\contracts\Cartography.Globe\Cartography.Globe.csproj" />
    <!-- UnifyMaths.Numerics (FbmNoise3/GradientNoise3): consumed transitively via App.World.Rendering
         (BirthRoughnessProfile exposes FbmNoiseParams). Referenced directly here because the plugin
         manipulates BirthRoughnessProfile (and thus FbmNoiseParams) in Service.cs; the 0.1.5 FEED
         package predates the FbmNoise3 commit (@ae6bb90), so the source project ref is required in
         dev/test (UseProjectReferences=true). See App.World.Rendering.csproj for the republish note. -->
    <ProjectReference Include="$(PlateProjectsRoot)\unify-maths\dotnet\src\UnifyMaths.Numerics\UnifyMaths.Numerics.csproj" />
  </ItemGroup>

  <PropertyGroup>
    <!-- NU1603 (UnifyGeometry floor versions vs the 1.0.0 feed line) is informational, non-fatal. -->
    <NoWarn>$(NoWarn);NU1603</NoWarn>
  </PropertyGroup>
  <ItemGroup>
    <PackageReference Include="GiantCroissant.PluginArchi.Extensibility.Abstractions" />
    <PackageReference Include="GiantCroissant.PluginArchi.SourceGenerators" PrivateAssets="all" />
    <PackageReference Include="Akka" />
    <PackageReference Include="Microsoft.Extensions.Logging.Abstractions" />
    <PackageReference Include="Microsoft.Extensions.DependencyInjection" />
    <PackageReference Include="SurrealDb.Embedded.InMemory" />
    <PackageReference Include="SurrealDb.Net" />
    <PackageReference Include="UnifyStorage.Abstractions" />
    <PackageReference Include="UnifyStorage.Generators" PrivateAssets="all" />
    <PackageReference Include="UnifyStorage.Runtime.SurrealDb" />
    <!-- The ECS itself: CellElevationModel constructs an ArchSystemRunner directly (Godot-free,
         unit-testable) — same UnifyEcs+Arch stack App.Ecs uses; no second ECS, no hand-rolled world. -->
    <PackageReference Include="UnifyEcs.Core" />
    <PackageReference Include="UnifyEcs.Runtime.Arch" />
    <!-- Cartography impl (GlobeSurfaceBuilder, NoiseRelief) from the local feed; the two
         cartography contracts above stay project refs and carry the same assembly names. -->
    <PackageReference Include="GiantCroissant.FantaSim.Cartography.Globe.Core" />
  </ItemGroup>

  <!--
    Engine consumption — hybrid (see project/Directory.Build.props):
      * UseProjectReferences=false (release): published packages from the local feed.
      * UseProjectReferences=true  (dev): project references to fantasim-world `main`. Geosphere.Crust
        project-refs its own deps (Topology/Fields/TruthStream), so the whole closure flows as `main`
        projects — no package/project version mixing.
    Crust + Topology + Shared back the always-compiled WorldFunctionProvider; Fields/Parameters/
    TruthStream back the full WorldHistoryCoordinator composition.
  -->
  <ItemGroup Condition="'$(UseProjectReferences)' != 'true'">
    <PackageReference Include="GiantCroissant.FantaSim.Geosphere.Crust" />
    <PackageReference Include="GiantCroissant.FantaSim.Geosphere.Plate.Topology.Generation" />
    <PackageReference Include="GiantCroissant.FantaSim.Geosphere.Plate.Topology.Contracts" />
    <PackageReference Include="GiantCroissant.FantaSim.Geosphere.Asthenosphere.Convection" />
    <PackageReference Include="GiantCroissant.FantaSim.Geosphere.Asthenosphere.Contracts" />
    <PackageReference Include="GiantCroissant.FantaSim.Atmosphere.Genesis.Core" />
    <PackageReference Include="GiantCroissant.FantaSim.Atmosphere.Contracts" />
    <PackageReference Include="GiantCroissant.FantaSim.Geosphere.Plate.Topology.Materializer" />
    <PackageReference Include="GiantCroissant.FantaSim.Geosphere.Plate.Rotation.Stream" />
    <PackageReference Include="GiantCroissant.FantaSim.Geosphere.Plate.Reconstruction" />
    <PackageReference Include="GiantCroissant.FantaSim.World.Shared.Contracts" />
    <PackageReference Include="GiantCroissant.FantaSim.World.TruthStream.Contracts" />
    <PackageReference Include="GiantCroissant.FantaSim.World.TruthStream.Core" />
    <PackageReference Include="GiantCroissant.FantaSim.World.Fields.Contracts" />
    <PackageReference Include="GiantCroissant.FantaSim.World.Fields.Core" />
    <PackageReference Include="GiantCroissant.FantaSim.World.Fields.Catalog" />
    <PackageReference Include="GiantCroissant.FantaSim.World.Parameters.Contracts" />
  </ItemGroup>
  <!-- engine: post-Plan-1-3 + atmosphere (codev) -->
  <ItemGroup Condition="'$(UseProjectReferences)' == 'true'">
    <ProjectReference Include="$(FantaSimWorldRoot)\project\plugins\Geosphere.Crust\Geosphere.Crust.csproj" />
    <ProjectReference Include="$(FantaSimWorldRoot)\project\plugins\Geosphere.Plate.Topology.Generation\Geosphere.Plate.Topology.Generation.csproj" />
    <ProjectReference Include="$(FantaSimWorldRoot)\project\contracts\Geosphere.Plate.Topology\Geosphere.Plate.Topology.csproj" />
    <ProjectReference Include="$(FantaSimWorldRoot)\project\plugins\Geosphere.Plate.Topology.Materializer\Geosphere.Plate.Topology.Materializer.csproj" />
    <ProjectReference Include="$(FantaSimWorldRoot)\project\plugins\Geosphere.Plate.Rotation.Stream\Geosphere.Plate.Rotation.Stream.csproj" />
    <ProjectReference Include="$(FantaSimWorldRoot)\project\plugins\Geosphere.Plate.Reconstruction\Geosphere.Plate.Reconstruction.csproj" />
    <ProjectReference Include="$(FantaSimWorldRoot)\project\plugins\Geosphere.Asthenosphere.Convection\Geosphere.Asthenosphere.Convection.csproj" />
    <ProjectReference Include="$(FantaSimWorldRoot)\project\contracts\Geosphere.Asthenosphere\Geosphere.Asthenosphere.csproj" />
    <ProjectReference Include="$(FantaSimWorldRoot)\project\plugins\Atmosphere.Genesis.Core\Atmosphere.Genesis.Core.csproj" />
    <ProjectReference Include="$(FantaSimWorldRoot)\project\contracts\Atmosphere\Atmosphere.csproj" />
    <ProjectReference Include="$(FantaSimWorldRoot)\project\contracts\World.Shared\World.Shared.csproj" />
    <ProjectReference Include="$(FantaSimWorldRoot)\project\contracts\World.Fields\World.Fields.csproj" />
    <ProjectReference Include="$(FantaSimWorldRoot)\project\plugins\World.Fields.Core\World.Fields.Core.csproj" />
    <ProjectReference Include="$(FantaSimWorldRoot)\project\contracts\World.Parameters\World.Parameters.csproj" />
    <ProjectReference Include="$(FantaSimWorldRoot)\project\contracts\World.TruthStream\World.TruthStream.csproj" />
    <ProjectReference Include="$(FantaSimWorldRoot)\project\plugins\World.TruthStream.Core\World.TruthStream.Core.csproj" />
    <!-- Cosmology hybrid (project mode): contracts are shared (resident host parent ALC),
         registry + science are collectible implementation companions staged into world.pck. -->
    <ProjectReference Include="$(FantaSimWorldRoot)\project\contracts\Mythosphere.Cosmology\Mythosphere.Cosmology.csproj" />
    <ProjectReference Include="$(FantaSimWorldRoot)\project\plugins\Mythosphere.Cosmology.Registry\Mythosphere.Cosmology.Registry.csproj" />
    <ProjectReference Include="$(FantaSimWorldRoot)\project\plugins\Mythosphere.Cosmology.Science\Mythosphere.Cosmology.Science.csproj" />
  </ItemGroup>

  <!-- Cosmology hybrid (package mode): published 0.1.14 packages from the local feed. -->
  <ItemGroup Condition="'$(UseProjectReferences)' != 'true'">
    <PackageReference Include="GiantCroissant.FantaSim.Mythosphere.Cosmology.Contracts" />
    <PackageReference Include="GiantCroissant.FantaSim.Mythosphere.Cosmology.Registry" />
    <PackageReference Include="GiantCroissant.FantaSim.Mythosphere.Cosmology.Science" />
  </ItemGroup>

</Project>
