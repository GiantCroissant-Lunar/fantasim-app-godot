<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <TargetFramework>net8.0</TargetFramework>
    <RootNamespace>FantaSim.App.World</RootNamespace>
    <AssemblyName>FantaSim.App.World</AssemblyName>
    <ServiceArchiTier>T3</ServiceArchiTier>
  </PropertyGroup>
  <ItemGroup>
    <CompilerVisibleProperty Include="ServiceArchiTier" />
  </ItemGroup>

  <!-- App.World.Tests constructs WorldRuntime with explicit descriptors to prove the startup
       catalog validation contract (duplicate FieldId throws). No other assembly gains access. -->
  <ItemGroup>
    <InternalsVisibleTo Include="App.World.Tests" />
  </ItemGroup>

  <!--
    T3 world orchestrator. App-side T1 contracts are always referenced. World-lib
    packages are referenced only when UseProjectReferences=true, so the default
    build compiles a stub service that does not touch world-lib types; the full
    composition path is compiled under USE_PROJECT_REFERENCES.
  -->
  <PropertyGroup Condition="'$(UseProjectReferences)' == 'true'">
    <DefineConstants>$(DefineConstants);USE_PROJECT_REFERENCES</DefineConstants>
  </PropertyGroup>

  <ItemGroup>
    <ProjectReference Include="..\..\contracts\App.World\App.World.csproj" />
    <ProjectReference Include="..\..\contracts\App.NodeGraph\App.NodeGraph.csproj" />
    <ProjectReference Include="..\..\contracts\App.Ecs\App.Ecs.csproj" />
  </ItemGroup>

  <!--
    Crust pipeline as a node-function provider (pure C#). Referenced ALWAYS — the
    WorldFunctionProvider + CrustGenerationGraph recipe compile in the default headless
    build and are exercised by App.World.Tests via `dotnet test` (no Godot, no UseProjectReferences).
    The crust package pulls its full world-lib + geometry dependency closure transitively.
    NU1603 (UnifyGeometry floor versions vs the 1.0.0 feed line) is informational and non-fatal.
  -->
  <PropertyGroup>
    <NoWarn>$(NoWarn);NU1603</NoWarn>
  </PropertyGroup>
  <ItemGroup>
    <PackageReference Include="GiantCroissant.FantaSim.Geosphere.Crust" />
    <PackageReference Include="GiantCroissant.FantaSim.Geosphere.Plate.Topology" />
    <PackageReference Include="GiantCroissant.FantaSim.World.Shared.Contracts" />
    <PackageReference Include="Microsoft.Extensions.Logging.Abstractions" />
  </ItemGroup>

  <!--
    World-lib packages for the full WorldRuntime composition path (only that path uses them
    directly; the crust packages above already bring the field/stream stack transitively).
  -->
  <ItemGroup Condition="'$(UseProjectReferences)' == 'true'">
    <PackageReference Include="GiantCroissant.FantaSim.World.Fields.Contracts" />
    <PackageReference Include="GiantCroissant.FantaSim.World.Fields.Core" />
    <PackageReference Include="GiantCroissant.FantaSim.World.Parameters.Contracts" />
    <PackageReference Include="GiantCroissant.FantaSim.World.TruthStream.Contracts" />
    <PackageReference Include="GiantCroissant.FantaSim.World.TruthStream.Core" />
  </ItemGroup>

</Project>
