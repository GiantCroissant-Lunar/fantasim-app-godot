using FantaSim.App.Audio;

namespace FantaSim.App.Audio.Services;

// ServiceArchi Tier 3: the concrete service implementation (placeholder).
// Will implement IService and self-register (e.g. [AutoRegister(typeof(IService))]);
// the T2 proxy in the contracts project resolves this from the registry.
public sealed partial class Service : IService
{
}
